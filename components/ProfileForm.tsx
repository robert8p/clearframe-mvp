"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export function ProfileForm({initialName,initialIndustry,initialRole}:{initialName:string;initialIndustry:string;initialRole:string}){const[name,setName]=useState(initialName);const[industry,setIndustry]=useState(initialIndustry);const[role,setRole]=useState(initialRole);const[msg,setMsg]=useState("");async function save(){const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user)return;const{error}=await s.from("profiles").update({full_name:name,industry,job_role:role}).eq("id",user.id);setMsg(error?error.message:"Saved.");}return <section className="card" style={{maxWidth:620}}><label>Name</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/><label>Industry (optional)</label><input className="input" value={industry} onChange={e=>setIndustry(e.target.value)} placeholder="Construction, finance, legal…"/><label>Role (optional)</label><input className="input" value={role} onChange={e=>setRole(e.target.value)} placeholder="Commercial director, analyst…"/><button className="button" style={{marginTop:18}} onClick={save}>Save profile</button>{msg&&<p className="muted">{msg}</p>}</section>}
