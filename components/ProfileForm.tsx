"use client";
import{FormEvent,useState}from"react";import{createClient}from"@/lib/supabase/client";import{AudienceSelector}from"@/components/AudienceSelector";import{ContextProfileForm}from"@/components/ContextProfileForm";import type{AudienceSegment}from"@/lib/audience";import type{ContextProfile}from"@/lib/context-profile";
export function ProfileForm({initialName,initialRole,initialAudience,initialContext}:{initialName:string;initialRole:string;initialAudience:AudienceSegment|null;initialContext:ContextProfile}){const[name,setName]=useState(initialName),[role,setRole]=useState(initialRole),[message,setMessage]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();if(busy)return;setBusy(true);setMessage("");setError("");try{const supabase=createClient();const{data:{user}}=await supabase.auth.getUser();if(!user){setError("Your session has expired. Sign in again to update your profile.");return}const{error:updateError}=await supabase.from("profiles").update({full_name:name.trim(),job_role:role.trim(),updated_at:new Date().toISOString()}).eq("id",user.id);if(updateError)setError("Could not save your profile details. Try again.");else setMessage("Profile details saved.")}catch{setError("Connection interrupted. Try saving again.")}finally{setBusy(false)}}return <><section className="cg-card cg-form-card" id="learning-context"><div className="cg-kicker">Learning context</div><h2>Make training fit your stage</h2><p>Change this whenever your situation changes. Future scenarios change; your past answers, XP and skill scores stay intact.</p><AudienceSelector compact initialValue={initialAudience}/></section>{initialAudience&&<section className="cg-card cg-form-card" id="personalisation"><div className="cg-kicker">Personalise scenarios</div><h2>Tell Cogni what is most relevant</h2><p>Optional details help Cogni prefer scenarios from your function, industry and current goal.</p><ContextProfileForm audience={initialAudience} initial={initialContext}/></section>}<section className="cg-card cg-form-card"><div className="cg-kicker">Profile details</div><form onSubmit={save}><label htmlFor="profile-name">Name</label><input id="profile-name" className="input" autoComplete="name" value={name} onChange={(event)=>setName(event.target.value)} required/><label htmlFor="profile-role">Role / title <span className="cg-field-help">optional</span></label><input id="profile-role" className="input" autoComplete="organization-title" value={role} onChange={(event)=>setRole(event.target.value)} placeholder="Analyst, engineering manager, CFO…"/><button type="submit" className="cg-button cg-full" style={{marginTop:18}} disabled={busy}>{busy?"Saving…":"Save profile details"}</button>{message&&<p className="cg-form-message" aria-live="polite">{message}</p>}{error&&<div className="cg-inline-error" role="alert">{error}</div>}</form></section></>}
