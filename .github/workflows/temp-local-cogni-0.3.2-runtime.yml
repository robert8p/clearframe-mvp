name: Locally rebuild and runtime-test Cogni 0.3.2

on:
  pull_request:
    paths:
      - "mobile/**"
      - ".github/scripts/verify-cogni-apk-runtime.sh"
      - ".github/workflows/temp-local-cogni-0.3.2-runtime.yml"

permissions:
  contents: read

jobs:
  local-build-and-inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    defaults:
      run:
        working-directory: mobile
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      EXPO_PUBLIC_SUPABASE_URL: https://dhklfrqhsmofqrawfdjz.supabase.co
      EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY: sb_publishable_Y1pPv4wp0DcQuq3XCEB0GA_mJtNPE2b
      EAS_LOCAL_BUILD_SKIP_CLEANUP: "1"
      EAS_LOCAL_BUILD_WORKINGDIR: /tmp/eas-local-build
      EAS_LOCAL_BUILD_ARTIFACTS_DIR: /tmp/eas-local-artifacts
      CI: "1"
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: "mobile/package-lock.json"

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - name: Install Android build prerequisites
        run: |
          yes | sdkmanager --licenses >/dev/null
          sdkmanager \
            "platforms;android-36" \
            "build-tools;36.0.0" \
            "ndk;27.1.12297006" \
            "cmake;3.22.1"
          sudo apt-get update
          sudo apt-get install -y ninja-build

      - name: Require Expo token
        run: |
          test -n "$EXPO_TOKEN"

      - name: Clean dependency install and source validation
        run: |
          rm -rf node_modules .expo android
          npm ci --include=dev --no-audit --no-fund
          npx expo install --check
          npm run lint
          npm run ui-audit
          npm run logic-audit
          npm run typecheck

      - name: Prove release configuration is compiled
        run: |
          rm -rf /tmp/cogni-export /tmp/cogni-export-strings.txt
          npx expo export --platform android --output-dir /tmp/cogni-export --clear
          find /tmp/cogni-export -type f -print0 | xargs -0 strings -a > /tmp/cogni-export-strings.txt
          grep -Fq "$EXPO_PUBLIC_SUPABASE_URL" /tmp/cogni-export-strings.txt
          grep -Fq "$EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY" /tmp/cogni-export-strings.txt

      - name: Perform full signed EAS build locally
        run: |
          rm -rf /tmp/eas-local-build /tmp/eas-local-artifacts /tmp/cogni-0.3.2-local.apk
          mkdir -p /tmp/eas-local-artifacts
          npx eas-cli@21.7.1 build \
            --platform android \
            --profile localVerification \
            --local \
            --non-interactive \
            --output /tmp/cogni-0.3.2-local.apk
          test -s /tmp/cogni-0.3.2-local.apk

      - name: Inspect the exact locally rebuilt APK
        shell: bash
        run: |
          set -euo pipefail
          APK=/tmp/cogni-0.3.2-local.apk
          unzip -t "$APK" >/dev/null
          unzip -Z1 "$APK" > /tmp/apk-files.txt

          AAPT=$(find "$ANDROID_HOME/build-tools" -type f -name aapt | sort -V | tail -1)
          APKSIGNER=$(find "$ANDROID_HOME/build-tools" -type f -name apksigner | sort -V | tail -1)
          ZIPALIGN=$(find "$ANDROID_HOME/build-tools" -type f -name zipalign | sort -V | tail -1)

          "$AAPT" dump badging "$APK" > /tmp/apk-badging.txt
          "$AAPT" dump permissions "$APK" > /tmp/apk-permissions.txt
          "$APKSIGNER" verify --verbose --print-certs "$APK" > /tmp/apk-signature.txt
          "$ZIPALIGN" -c -P 16 4 "$APK"

          grep -q "package: name='app.gocogni.cogni'" /tmp/apk-badging.txt
          grep -q "versionName='0.3.2'" /tmp/apk-badging.txt
          grep -q "application-label:'Cogni'" /tmp/apk-badging.txt
          grep -q "targetSdkVersion:'36'" /tmp/apk-badging.txt
          grep -q "android.permission.INTERNET" /tmp/apk-permissions.txt
          ! grep -q "android.permission.SYSTEM_ALERT_WINDOW" /tmp/apk-permissions.txt
          ! grep -q "android.permission.READ_EXTERNAL_STORAGE" /tmp/apk-permissions.txt
          ! grep -q "android.permission.WRITE_EXTERNAL_STORAGE" /tmp/apk-permissions.txt
          grep -q "Verified using v2 scheme (APK Signature Scheme v2): true" /tmp/apk-signature.txt

          NORMALIZED_SIGNATURE=$(tr -d ':' < /tmp/apk-signature.txt | tr '[:upper:]' '[:lower:]')
          grep -q "e0bfda379dfa0e11aee798e443ce3b33d006d83b7857062f8f639ca7f7572c7e" <<<"$NORMALIZED_SIGNATURE"
          grep -q '^lib/x86_64/' /tmp/apk-files.txt
          grep -q '^lib/arm64-v8a/' /tmp/apk-files.txt

          BUNDLE_PATH=$(awk '/(^|\/)index\.android\.bundle$/{print; exit}' /tmp/apk-files.txt)
          test -n "$BUNDLE_PATH"
          unzip -p "$APK" "$BUNDLE_PATH" > /tmp/index.android.bundle
          strings -a /tmp/index.android.bundle > /tmp/index.android.strings.txt
          grep -Fq "$EXPO_PUBLIC_SUPABASE_URL" /tmp/index.android.strings.txt
          grep -Fq "$EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY" /tmp/index.android.strings.txt

          SHA256=$(sha256sum "$APK" | awk '{print $1}')
          VERSION_CODE=$(sed -n "s/.*versionCode='\([^']*\)'.*/\1/p" /tmp/apk-badging.txt | head -1)
          echo "APK_SHA256=$SHA256"
          echo "APK_VERSION_CODE=$VERSION_CODE"

          mkdir -p /tmp/cogni-local-artifact
          cp "$APK" /tmp/cogni-local-artifact/cogni-0.3.2.apk
          cp /tmp/apk-badging.txt /tmp/cogni-local-artifact/
          cp /tmp/apk-permissions.txt /tmp/cogni-local-artifact/
          cp /tmp/apk-signature.txt /tmp/cogni-local-artifact/
          printf '{"version":"0.3.2","versionCode":"%s","sha256":"%s","sourceCommit":"%s"}\n' \
            "$VERSION_CODE" "$SHA256" "$GITHUB_SHA" > /tmp/cogni-local-artifact/metadata.json

      - name: Upload the exact inspected APK
        uses: actions/upload-artifact@v4
        with:
          name: cogni-0.3.2-local-signed-apk
          path: /tmp/cogni-local-artifact
          if-no-files-found: error
          retention-days: 7

      - name: Preserve local-build logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cogni-0.3.2-local-build-debug
          path: /tmp/eas-local-build
          if-no-files-found: warn
          retention-days: 7

  runtime-test:
    needs: local-build-and-inspect
    runs-on: ubuntu-latest
    timeout-minutes: 50
    strategy:
      fail-fast: false
      matrix:
        api-level: [35, 36]
    steps:
      - uses: actions/checkout@v6

      - name: Download the exact inspected APK
        uses: actions/download-artifact@v4
        with:
          name: cogni-0.3.2-local-signed-apk
          path: /tmp/cogni-local-artifact

      - name: Enable KVM acceleration
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          ls -l /dev/kvm

      - name: Clean-install and cold-launch on Android API ${{ matrix.api-level }}
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: ${{ matrix.api-level }}
          target: google_apis
          arch: x86_64
          profile: pixel_5
          cores: 4
          ram-size: 3072M
          heap-size: 512M
          disk-size: 6G
          emulator-boot-timeout: 900
          disable-linux-hw-accel: false
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim -camera-back none -camera-front none
          script: APK_PATH=/tmp/cogni-local-artifact/cogni-0.3.2.apk API_LEVEL=${{ matrix.api-level }} bash .github/scripts/verify-cogni-apk-runtime.sh

      - name: Preserve runtime evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cogni-0.3.2-local-runtime-api-${{ matrix.api-level }}
          path: /tmp/cogni-runtime-api-${{ matrix.api-level }}
          if-no-files-found: warn
          retention-days: 7
