import { requireUser } from "@/lib/auth";import { SkillBars } from "@/components/SkillBars";
export default async function Skills(){const {user,supabase}=await requireUser();const {data}=await supabase.from("user_skill_scores").select("score,reliability,skills(name,slug)").eq("user_id",user.id).order("score");return <><div className="kicker">Capability profile</div><h1>Your development scores</h1><p className="muted">Scores update from observed performance. Reliability is shown separately so early estimates are not presented with false precision.</p><SkillBars rows={(data??[]) as never[]}/></>}
