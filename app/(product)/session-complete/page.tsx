import Link from"next/link";import{redirect}from"next/navigation";import{requireUser}from"@/lib/auth";import{SessionCelebration}from"@/components/SessionCelebration";import{SessionFeedback}from"@/components/SessionFeedback";import{patternCopy,sessionInsight,type ErrorPatternCount}from"@/lib/insights";import{audienceOutcome,audienceTransferStatement,isAudienceSegment}from"@/lib/audience";
type SkillMovement={name:string;before:number|null;after:number;delta:number|null;touches:number};type NestedSkill={name?:string;slug?:string};function nestedSkill(value:unknown):NestedSkill{if(Array.isArray(value))return(value[0]as NestedSkill|undefined)??{};if(value&&typeof value==="object")return value as NestedSkill;return{}}function nestedSkillName(v:unknown){return nestedSkill(v).name??"Skill"}function askRelevance(id:string){return id.split("").reduce((sum,c)=>sum+c.charCodeAt(0),0)%3===0}
export default async function SessionCompletePage(){const{user,supabase}=await requireUser();const[{data:profile},{data:session},{data:scoreRows}]=await Promise.all([supabase.from("profiles").select("xp,current_streak,audience_segment").eq("id",user.id).single(),supabase.from("training_sessions").select("id,completed_at").eq("user_id",user.id).eq("status","completed").order("completed_at",{ascending:false}).limit(1).maybeSingle(),supabase.from("user_skill_scores").select("skill_id,score,skills(name,slug)").eq("user_id",user.id).gt("attempts",0)]);if(!session?.id)redirect("/dashboard");const{data:responseRows}=await supabase.from("user_responses").select("id,challenge_id,is_correct,score_fraction,confidence,error_pattern,xp_awarded,created_at").eq("user_id",user.id).eq("session_key",session.id).order("created_at"),responses=responseRows??[];let movements:SkillMovement[]=[];const responseIds=responses.map((r:any)=>String(r.id));if(responseIds.length){const{data:updateRows}=await supabase.from("user_response_skill_updates").select("skill_id,score_before,score_after,created_at,skills(name)").eq("user_id",user.id).in("response_id",responseIds).order("created_at");if(updateRows?.length){const grouped=new Map<string,SkillMovement>();for(const row of updateRows as any[]){const id=String(row.skill_id),existing=grouped.get(id),before=Number(row.score_before),after=Number(row.score_after);if(!existing)grouped.set(id,{name:nestedSkillName(row.skills),before,after,delta:Number((after-before).toFixed(1)),touches:1});else{existing.after=after;existing.delta=existing.before===null?null:Number((after-existing.before).toFixed(1));existing.touches+=1}}movements=[...grouped.values()].sort((a,b)=>Math.abs(b.delta??0)-Math.abs(a.delta??0))}}const total=responses.length,fullyCorrect=responses.filter((r:any)=>Boolean(r.is_correct)).length,sessionScore=total?Math.round(responses.reduce((sum:number,r:any)=>sum+Number(r.score_fraction??(r.is_correct?1:0)),0)/total*100):0,confidenceValues=responses.map((r:any)=>r.confidence).filter((v:unknown):v is number=>typeof v==="number"),averageConfidence=confidenceValues.length?Math.round(confidenceValues.reduce((s,v)=>s+v,0)/confidenceValues.length):0,sessionXp=responses.reduce((sum:number,r:any)=>sum+Number(r.xp_awarded??(r.is_correct?12:7)),0),patternMap=new Map<string,number>();for(const row of responses)if(row.error_pattern)patternMap.set(String(row.error_pattern),(patternMap.get(String(row.error_pattern))??0)+1);const patterns:ErrorPatternCount[]=[...patternMap.entries()].map(([pattern,count])=>({pattern,count})).sort((a,b)=>b.count-a.count),focusSkill=movements[0]?.name??null,insight=sessionInsight({accuracy:sessionScore,averageConfidence,patterns,focusSkill}),audience=isAudienceSegment(profile?.audience_segment)?profile.audience_segment:null,completionTitle=audience?audienceOutcome(audience):sessionScore>=80?"Excellent!":sessionScore>=60?"Strong session!":"Session complete!",weakestRow=[...(scoreRows??[])].sort((a:any,b:any)=>Number(a.score)-Number(b.score))[0]as any,weakestSkill=nestedSkill(weakestRow?.skills),extraTrainingHref=weakestSkill.slug?`/practice/${weakestSkill.slug}`:"/skills",transfer=audience?audienceTransferStatement(audience,focusSkill??undefined):null;return <div className={`cg-mobile-page cg-results-screen ${audience==="executive"?"cg-executive-results":""}`}><SessionCelebration xp={sessionXp} streak={profile?.current_streak??0} title={completionTitle}/><p className="cg-results-scoreline">{fullyCorrect} fully correct • {sessionScore}% session score across all question types.</p>{transfer&&<section className="cg-card cg-transfer-card"><div className="cg-kicker">Transfer to your world</div><p>{transfer}</p></section>}<div className="cg-result-stats"><div><small>Score</small><strong>{sessionScore}%</strong></div><div><small>Confidence</small><strong>{averageConfidence}%</strong></div><div><small>Streak</small><strong>{profile?.current_streak??0}</strong></div></div><section className="cg-card cg-personal-insight-card"><div className="cg-kicker">Your insight</div><h2>What this session suggests</h2><p>{insight}</p></section><section className="cg-card"><div className="cg-section-head flush"><h2>Skills updated</h2><span className="cg-pill">{movements.length}</span></div><div className="cg-session-skill-list">{movements.length?movements.slice(0,5).map(m=><div className="cg-session-skill" key={m.name}><div><strong>{m.name}</strong><small>{m.touches>1?`${m.touches} questions`:"1 question"}</small></div><div className="cg-session-skill-score"><strong>{Math.round(m.after)}</strong>{m.delta===null?<span className="cg-delta neutral">practised</span>:<span className={`cg-delta ${m.delta>0?"up":m.delta<0?"down":"neutral"}`}>{m.delta>0?"+":""}{m.delta.toFixed(1)}</span>}</div></div>):<p>No skill change was recorded for this session.</p>}</div></section><section className="cg-card"><div className="cg-section-head flush"><h2>What to learn from</h2><span className="cg-pill">{patterns.reduce((sum,item)=>sum+item.count,0)}</span></div>{patterns.length?<div className="cg-pattern-list">{patterns.map(item=>{const copy=patternCopy(item.pattern);return <div className="cg-pattern-row" key={item.pattern}><div><strong>{copy?.label??item.pattern.replaceAll("_"," ")}</strong><small>{copy?.action}</small></div><span className="cg-pill">×{item.count}</span></div>})}</div>:<div className="cg-clean-session"><strong>No repeated thinking mistake showed up.</strong><p>That does not mean the skill is mastered; it just means no clear repeated mistake appeared today.</p></div>}</section><section className="cg-card cg-session-footer-card"><div><div className="cg-kicker">Total XP</div><strong>{profile?.xp??0}</strong></div><div><div className="cg-kicker">Next skill</div><strong>{weakestSkill.name??focusSkill??"Keep practising"}</strong></div></section><section className="cg-card cg-extra-training-card"><div className="cg-kicker">Want to keep going?</div><h2>Daily goal complete. Training stays open.</h2><p>{weakestSkill.name?`Take 3 fresh questions focused on ${weakestSkill.name}.`:"Choose a skill and continue with fresh questions."} Extra practice is optional and still updates your skill scores.</p><Link href={extraTrainingHref} className="cg-button cg-full">Keep training</Link></section><SessionFeedback askRelevance={askRelevance(session.id)}/><div className="cg-results-actions"><Link href="/dashboard" className="cg-button cg-full">Back to Home</Link><Link href="/skills" className="cg-button secondary cg-full">Choose another skill</Link></div></div>}
