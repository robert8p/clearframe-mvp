import {requireAdmin} from "@/lib/auth";import{AdminChallengeForm}from"@/components/AdminChallengeForm";
export default async function Admin(){const{supabase}=await requireAdmin();const{data}=await supabase.from("challenges").select("id,title,challenge_type,difficulty,is_published").order("created_at",{ascending:false}).limit(20);return <><div className="kicker">Internal</div><h1>Content management</h1><div className="grid grid-2"><AdminChallengeForm/><section className="card"><h2>Recent content</h2><table className="table"><thead><tr><th>Challenge</th><th>Type</th><th>Diff.</th></tr></thead><tbody>{(data??[]).map(x=><tr key={x.id}><td>{x.title}</td><td>{x.challenge_type}</td><td>{x.difficulty}</td></tr>)}</tbody></table></section></div></>}
