"use client";
import{useEffect,useMemo,useState}from"react";import{useRouter}from"next/navigation";import type{DailyLesson as DailyLessonType}from"@/lib/types";import type{AudienceSegment}from"@/lib/audience";
function track(eventName:string,properties:Record<string,unknown>={}){return fetch("/api/event",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({eventName,properties}),keepalive:true}).catch(()=>undefined)}
export function DailyLesson({lesson,audience,audienceLabel,questionCount,transferStatement}:{lesson:DailyLessonType;audience:AudienceSegment;audienceLabel:string;questionCount:number;transferStatement:string}){const[step,setStep]=useState(0),[reflection,setReflection]=useState(""),[revealed,setRevealed]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");const router=useRouter();const slides=useMemo(()=>[{kicker:"Today’s story",title:lesson.title,body:lesson.content.story,icon:lesson.emoji},{kicker:"The twist",title:"Here’s what makes it interesting",body:lesson.content.twist,icon:"⚡"},{kicker:"Key idea",title:lesson.subtitle,body:lesson.content.principle,icon:"🧠"},{kicker:"Your turn",title:"What do you think?",body:lesson.content.try_it,icon:"✍️",reflection:true},{kicker:"Use it in your world",title:"Why this matters to you",body:lesson.content.ai_age,icon:"✦",transfer:true}],[lesson]);const slide=slides[step],progress=Math.round(((step+1)/slides.length)*100);useEffect(()=>{void track("daily_lesson_started",{lesson_id:lesson.id,lesson_slug:lesson.slug,estimated_minutes:lesson.estimated_minutes,audience_segment:audience})},[lesson.id,lesson.slug,lesson.estimated_minutes,audience]);useEffect(()=>{void track("daily_lesson_step",{lesson_id:lesson.id,step:step+1,total_steps:slides.length,progress,audience_segment:audience})},[lesson.id,progress,slides.length,step,audience]);async function complete(){if(busy)return;setBusy(true);setError("");try{const response=await fetch("/api/lesson/complete",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({lessonId:lesson.id})});const body=await response.json().catch(()=>({}));if(!response.ok){setError(typeof body.error==="string"?body.error:"Could not complete today’s lesson. Try again.");return}const earned=Number(body.xpEarned??0);if(earned>0&&typeof navigator!=="undefined"&&typeof navigator.vibrate==="function")navigator.vibrate([18,35,22]);router.push("/training");router.refresh()}catch{setError("Connection interrupted. Your place in the lesson is still here — try again.")}finally{setBusy(false)}}function next(){setError("");if(step<slides.length-1){setStep(v=>v+1);return}void complete()}function back(){setError("");if(step>0&&!busy)setStep(v=>v-1)}function reveal(){setRevealed(true);void track("daily_lesson_reveal",{lesson_id:lesson.id,reflection_length:reflection.trim().length,audience_segment:audience})}
 const finishLabel=audience==="executive"?"Continue to decisions →":audience==="management"?"Continue to management scenarios →":"Finish lesson +5 XP →";
 return <div className="cg-lesson-screen"><div className="cg-lesson-topline"><div><span className="cg-kicker">{audienceLabel} insight</span><strong>{lesson.estimated_minutes} min</strong></div><span>{step+1}/{slides.length}</span></div><div className="progress cg-animated-progress" aria-label={`${progress}% through today's lesson`}><span style={{width:`${progress}%`}}/></div>{lesson.scenario_context&&<div className="cg-context-strip"><span>Situation</span><strong>{lesson.scenario_context.replace(/^.*?•\s*/,"")}</strong></div>}<section key={step} className="cg-lesson-story-card cg-question-stage"><div className="cg-lesson-emoji" aria-hidden="true">{slide.icon}</div><span className="cg-kicker">{slide.kicker}</span><h1>{slide.title}</h1><p>{slide.body}</p>{slide.reflection&&<div className="cg-reflection-box"><label htmlFor="lesson-reflection">Your first thought</label><textarea id="lesson-reflection" className="input" rows={4} value={reflection} onChange={e=>setReflection(e.target.value)} placeholder="Write one sentence. This isn’t saved or graded."/>{!revealed?<button type="button" className="cg-button secondary cg-full" disabled={reflection.trim().length<3} onClick={reveal}>Show the key idea</button>:<div className="cg-lesson-reveal"><span>Compare with the lesson</span><strong>{lesson.content.reveal}</strong></div>}</div>}{slide.transfer&&<div className="cg-transfer-callout"><strong>Why this matters to you</strong><p>{transferStatement}</p></div>}</section><div className="cg-lesson-dots" aria-hidden="true">{slides.map((_,index)=><i key={index} className={index<=step?"active":""}/>)}</div>{error&&<div className="cg-inline-error" role="alert">{error}</div>}<div className="cg-lesson-actions">{step>0?<button type="button" className="cg-button secondary" disabled={busy} onClick={back}>← Back</button>:<span/>}<button type="button" className="cg-button cg-full cg-lesson-next" onClick={next} disabled={busy||(slide.reflection&&!revealed)}>{busy?"Saving your lesson…":step===slides.length-1?finishLabel:"Continue →"}</button></div><p className="cg-lesson-footnote">One useful idea, then {questionCount} {audience==="executive"?"decision scenarios":"practice questions"}.</p></div>}
